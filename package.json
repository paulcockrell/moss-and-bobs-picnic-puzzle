{
    "name": "locksmith",
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "preview": "vite preview",
        "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
        "check": "tsc"
    },
    "dependencies": {
        "@kayahr/tiled": "^0.0.1",
        "kaplay": "^3001.0.6"
    },
    "devDependencies": {
        "@types/node": "^22.10.5",
        "typescript": "^5.7.3",
        "vite": "^6.0.7"
    }
}
